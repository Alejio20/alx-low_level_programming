#include <stdio.h>
#include <main.h>

/**
 * main - prints _putchar followed by a new line
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	_putcahr(a);
	_putchar(b);
	_putchar(c);
	_putchar(d);
	_putchar(e);
	_putchar(f);
	_putchar(g);
	_putchar(h);
	_putchar('\n');

	return (0);
}
