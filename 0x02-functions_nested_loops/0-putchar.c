#include <stdio.h>

/**
 * main - prints _putchar followed by a new line
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	char a, b, c, d, e, f, g, h;

	a = '_';
	b = 'p';
	c = 'u';
	d = 't';
	e = 'c';
	f = 'h';
	g = 'a';
	h = 'r';

	putcahr(a);
	putchar(b);
	putchar(c);
	putchar(d);
	putchar(e);
	putchar(f);
	putchar(g);
	putchar(h);
	putchar('\n');

	return (0);
}
